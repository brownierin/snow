rules:
- id: unescaped-data-in-js
  message: |
    Found a formatted template string passed to 'template.JS()'.
    'template.JS()' does not escape contents. Be absolutely sure
    there is no user-controlled data in this template.  JS is intended to encapsulate a known safe JavaScript expression.  Passing untrusted input to JS() can lead to Cross-Site Scripting (XSS) vulnerabilities.  Do not pass user controlled input to front end templates that will render as JavaScript.
  metadata:
    cwe: "CWE-79: Improper Neutralization of Input During Web Page Generation ('Cross-site Scripting')"
    owasp: 'A7: Cross-Site Scripting (XSS)'
    references:
    - https://golang.org/pkg/html/template/#JS
  languages: [go]
  severity: WARNING
  patterns:
  - pattern-either:
    - pattern: template.JS($T + $X, ...)
    - pattern: template.JS(fmt.$P("...", ...), ...)
    - pattern: |
        $T = "..."
        ...
        $T = $FXN(..., $T, ...)
        ...
        template.JS($T, ...)
    - pattern: |
        $T = fmt.$P("...", ...)
        ...
        template.JS($T, ...)
    - pattern: |
        $T, $ERR = fmt.$P("...", ...)
        ...
        template.JS($T, ...)
    - pattern: |
        $T = $X + $Y
        ...
        template.JS($T, ...)
    - pattern: |
        $T = "..."
        ...
        $OTHER, $ERR = fmt.$P(..., $T, ...)
        ...
        template.JS($OTHER, ...)
